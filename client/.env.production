REACT_APP_API_URL=/api

# Faster production builds (no browser source maps; use local dev or temporarily unset for prod debugging).
GENERATE_SOURCEMAP=false

# Skips ESLint during webpack build; keep lint in editor or run: cd client && npx eslint "src/**/*.{ts,tsx,js,jsx}"
DISABLE_ESLINT_PLUGIN=true
