node_modules
.vercel

# Local env (never commit secrets)
.env
**/.env
.env.local
.env.*.local

# macOS
.DS_Store

# Build output (regenerated by `npm run build`; Vercel builds from source)
client/build

# Local SQLite database used by the dev server
server/database/*.db
server/database/*.db-shm
server/database/*.db-wal
