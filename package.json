{
  "name": "poker-tracker-v2",
  "version": "1.0.0",
  "description": "A full-stack web application for tracking poker games, player statistics, and financial settlements",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "poker",
    "tracker",
    "react",
    "nodejs",
    "sqlite"
  ],
  "author": "Tanish Shah",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.11.0"
  }
}
